// JWT authentication middleware placeholder
// Implement verifyToken(req, res, next) to protect routes.

module.exports = {
  verifyToken: (req, res, next) => {
    // TODO: implement JWT verification
    next();
  }
};
